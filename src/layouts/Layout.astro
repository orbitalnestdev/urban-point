---
import '../styles/global.css';
import '../styles/design-system.css';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import DevUserSwitcher from '../components/DevUserSwitcher.astro';
import ImpersonationBanner from '../components/ImpersonationBanner.astro';
import { parseActiveNodeCookie } from '../lib/nodeSession';

interface Props {
	title: string;
}

const { title } = Astro.props;
const activeNodeSession = parseActiveNodeCookie(Astro.request.headers.get('cookie'));
---

<!doctype html>
<html lang="es" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="UrbanPoint - Comprá en línea y retirá tus pedidos cerca de tu casa. O sumate como Canillita y ganá comisiones." />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/jpeg" href="/images/favicon.jpg" />
		<link rel="preconnect" href="https://aw.orbitalnest.net" crossorigin>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@600;700;800&display=swap" rel="stylesheet">

		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="bg-[#F8FAFC] text-slate-900 font-sans antialiased selection:bg-indigo-500 selection:text-white flex flex-col min-h-screen">
		{activeNodeSession && (
			<div class="bg-[#1E3E1B] text-white px-4 py-2 text-xs font-bold flex items-center justify-center gap-2 text-center shadow-inner z-50">
				<span class="inline-block w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
				<span>Estás comprando en <strong>UrbanPoint — {activeNodeSession.nombre}</strong> ({activeNodeSession.direccion})</span>
				<a href={`/${activeNodeSession.slug}`} class="ml-2 underline hover:text-emerald-200 transition-colors">
					Ver punto &rarr;
				</a>
			</div>
		)}

		<ImpersonationBanner />
		<slot />
		<FloatingWhatsApp />
		<DevUserSwitcher />
		
		<script>
			// El código de referido lo persiste el middleware en una cookie
			// httpOnly (ver src/middleware.ts). Acá sólo se limpia la URL.
			//
			// Antes este script guardaba el código en localStorage y sólo lo
			// copiaba de la cookie cuando el store estaba vacío, imponiendo un
			// criterio first-touch que contradecía al last-touch de la página
			// del nodo: entrar por A y después por B hacía que A cobrara el
			// referido y B el fee de logística.
			const url = new URL(window.location.href);
			if (url.searchParams.has('ref')) {
				url.searchParams.delete('ref');
				window.history.replaceState({}, '', url);
			}
		</script>
	</body>
</html>
